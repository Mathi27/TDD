{
  "name": "tddpractice",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest --watchAll --verbose --coverage",
    "e2e":"cypress open"
  },
  "devDependencies": {
    "cypress": "^12.9.0",
    "vite": "^4.2.0"
  },
  "dependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0"
  }
}
